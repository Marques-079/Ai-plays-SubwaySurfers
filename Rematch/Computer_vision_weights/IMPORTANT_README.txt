

Currently the weights are all stored locally and the code calls them using local paths. 

jake_weights.txt is a placeholder for jake_weights.pt (file was too large at 128mb) ->might need to downsize later
